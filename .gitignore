__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/

# Poids/modèles : exclus par défaut, SAUF les artefacts légers versionnés
# (poids HJB ~150 KB nécessaires au benchmark HJB-PINN en ligne).
*.pt
!artifacts/*.pt

# Données : exclues par défaut, SAUF les CSV de démo (Maroc, légers).
data/*
!data/*.csv

# Bases locales / runtime
*.db
*.db-wal
*.db-shm

# Documents lourds (hors code)
*.pptx
*.pdf
*.docx
*.zip

.DS_Store
Thumbs.db

# Config Claude locale (propre a la machine)
.claude/settings.local.json

# Hors livrable : outils locaux et generation de documents (gardes sur disque,
# invisibles sur GitHub — le repo ne montre que la plateforme deployable).
.claude/
.vscode/
run_local.ps1
scripts/make_*.py
scripts/merge_slides.py
scripts/_pdf_assets/
scripts/_report_assets/
scripts/_slide_assets/
scripts/_slides_assets/
artifacts/hjb_value_maroc_seir_reconstruit.pt
data/maroc_donnees_reelles.csv
